# Snowflake Connection Configuration
# Copy this file to .env.local and fill in your actual values

# Snowflake Account Configuration
SNOWFLAKE_ACCOUNT=your-account.region.cloud       # e.g., abc12345.us-east-1.snowflakecomputing.com
SNOWFLAKE_USER=your-username                       # Your Snowflake username

# Authentication - use EITHER password OR PAT token (not both)
SNOWFLAKE_PASSWORD=your-password                   # Your Snowflake password
# SNOWFLAKE_PAT_TOKEN=your-pat-token               # Snowflake Personal Access Token (alternative to password)

SNOWFLAKE_WAREHOUSE=your-warehouse                 # Compute warehouse to use
SNOWFLAKE_DATABASE=your-database                   # Default database (will create PREFIX_database)
SNOWFLAKE_SCHEMA=PUBLIC                            # Default schema
SNOWFLAKE_ROLE=SYSADMIN                           # Role with sufficient privileges

# Skyflow Data Privacy Vault Configuration
SKYFLOW_VAULT_URL=https://your-vault.vault.skyflowapis.com  # Your Skyflow vault URL
SKYFLOW_VAULT_ID=your-vault-id                              # Your vault identifier
SKYFLOW_PAT_TOKEN=eyJhbGci...your-pat-token                 # Your Skyflow Personal Access Token
SKYFLOW_TABLE=pii                                           # Table name in Skyflow vault
SKYFLOW_TABLE_COLUMN=pii_values                             # Column name for PII data
SKYFLOW_BATCH_SIZE=25                                       # Batch size for tokenization operations

# Role Mappings for Data Access Control
# These correspond to Snowflake roles that should be created in your account
PLAIN_TEXT_GROUPS=AUDITOR                          # Role that sees detokenized (plain text) data
MASKED_GROUPS=CUSTOMER_SERVICE                     # Role that sees masked data (e.g., J****an)
REDACTED_GROUPS=MARKETING                          # Role that sees only tokens

# Optional: Logging Configuration
# LOG_LEVEL=INFO                                   # DEBUG, INFO, WARNING, ERROR